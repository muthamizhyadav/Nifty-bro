name: nifty_ai_bot
description: Nifty 50 AI Trading Bot — Mobile + Desktop Dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  web_socket_channel: ^3.0.1
  http: ^1.2.2
  intl: ^0.19.0
  provider: ^6.1.2
  url_launcher: ^6.3.0
  candlesticks: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true